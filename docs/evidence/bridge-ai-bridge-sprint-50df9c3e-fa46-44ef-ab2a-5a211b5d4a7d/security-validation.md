# Security validation

`ExecutionProvider.clean` rejects invalid credential references and mismatched provider kinds/roles. `public_provider` explicitly excludes configuration and credential data. The complete repository lint, type, and test gates passed on the final state.
