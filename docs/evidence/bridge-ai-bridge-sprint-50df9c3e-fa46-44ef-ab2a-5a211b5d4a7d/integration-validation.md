# Integration validation

Migration `0011_executionprovider_providerauditevent` seeds Codex as the operational execution provider and registers OpenAI, Claude, GitHub, and BigQuery as disabled DRAFT records. The existing consumed-contract receipt identity continues to drive dispatch. The complete suite passed with 51 tests.
