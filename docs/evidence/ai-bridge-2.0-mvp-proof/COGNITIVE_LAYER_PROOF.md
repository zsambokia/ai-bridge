# Cognitive Layer Proof

## Result: PASS

`projects/tests/test_cognitive_evolution.py` proves that a verified Runtime
reflection becomes a project-scoped `CognitiveExperience`, then an inert
`BehaviourCandidate`, and becomes guidance only after explicit governance.

Phase 10 repeats Runtime execution after semantic reconstruction, records both
reflections, creates an approved behaviour candidate for each, and builds
guidance. Both approved patterns retain the
`verified-container-calculation` strategy and metrics report two approved
patterns. Semantic rebuilding therefore does not impair governed cognitive
evolution.
