# Zoning conformance

Zoning is checked before both forward and return delivery. Default is deny: an exact ALLOW is required and any exact DENY wins. It is scoped to the Effective Operational Scope, does not replace domain authorization, and grants no state-mutation authority.
