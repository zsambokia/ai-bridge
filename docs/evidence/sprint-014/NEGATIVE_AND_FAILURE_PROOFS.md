# Negative and failure proofs

Exact provider selection rejects non-selected identities; disabled/draft providers are rejected; unsupported capability selection is rejected; absent credential references are unavailable; BigQuery rejects non-read queries. These outcomes are deterministic and do not fall back to Codex or another provider.
