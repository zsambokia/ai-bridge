# OpenAI provider proof

The provider is implemented as a bounded Responses API model adapter and remains DRAFT/disabled. No `OPENAI` credential reference was available, so no remote call was attempted or claimed. Missing bindings produce `PROVIDER_CREDENTIAL_BINDING_REQUIRED` or `PROVIDER_CREDENTIAL_UNAVAILABLE` without dispatch.
