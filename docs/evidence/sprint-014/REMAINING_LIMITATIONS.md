# Remaining limitations

No OpenAI, Claude, GitHub, or BigQuery credentials or safe external proof targets were supplied. Those providers remain disabled DRAFT records and have no claimed live proof. The provider platform does not introduce policy fallback: an exact contract provider continues to fail rather than silently selecting another provider.
