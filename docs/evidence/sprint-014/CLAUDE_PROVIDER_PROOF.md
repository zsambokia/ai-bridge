# Claude provider proof

The provider is implemented as a bounded Messages API model adapter and remains DRAFT/disabled. No Claude credential reference was available, so the truthful result is configured-but-unavailable; no external success is claimed.
