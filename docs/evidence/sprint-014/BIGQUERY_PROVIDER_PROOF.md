# BigQuery provider proof

BigQuery is classified solely as `DATA_SERVICE`. The adapter accepts only bounded `SELECT`/`WITH` reads and rejects write-shaped input. No credentials or safe dataset were supplied, so no external query was attempted or claimed.
