# Previous reconstruction gap report

## Comparison boundary

This comparison is performed only after the independent, source-derived package
was written. It does not use earlier reconstruction documents as decision
evidence.

## Source-demonstrated deficiencies requiring correction

The local conversation itself records that the earlier reconstruction was not
sufficiently detailed, needed a thorough rework, and contained semantic
decision-ID collision discovered during random checking (R-31; especially
CHAT-0396, CHAT-0400, CHAT-0418, CHAT-0422).

Accordingly, this package corrects the method rather than attempting to preserve
old labels:

| Earlier-risk pattern | Correction in this package |
|---|---|
| Existing ID determines meaning | R-01…R-31 were assigned after chronological source reconstruction. |
| Keyword/supporting-quote selection | Timeline, lineage, approvals, rejections, open questions, and negative invariants are separated. |
| Lost diagram context | 106 Mermaid blocks are registered with reproducible locators. |
| Fragile source reference | `CHAT-####` normalizer maps to source marker order and manifest identity. |
| Baseline treated as target | Current Constitution is explicitly a baseline comparison, not source authority. |

## Process-integrity limitation

Before this replacement instruction was operationally applied, the existing
`ARCHITECTURE_CONVERGENCE_PROGRAM_MASTER_PLAN.md` was opened while establishing
repository methodology. This contradicts the later quarantine completion gate.
No target claim from it was used to construct the R ledger, but the gate cannot
be represented as passed. This is recorded transparently in the execution and
verification records.

## Remaining comparison work

A forensic, claim-by-claim comparison of every old derived document may be run
in a future approved audit. It must preserve this package as the primary,
source-derived reference and must not retroactively erase the above integrity
exception.
