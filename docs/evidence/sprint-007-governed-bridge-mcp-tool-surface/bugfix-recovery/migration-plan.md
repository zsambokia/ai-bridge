# Migration plan

Apply the already-versioned additive Django migration with `python manage.py
migrate`, then verify `[X] 0005_governed_mcp_records` with `python manage.py
showmigrations projects`. The complete operator procedure and restart guidance
are in `STAGING_DEPLOYMENT_RUNBOOK.md`.
