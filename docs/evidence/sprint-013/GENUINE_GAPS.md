# Genuine gaps

The assessment found two tightly coupled gaps:

1. `AUDIT` was not a first-class proposal work type.
2. Provider selection was hard-coded at orchestration and execution resolution,
   rather than being explicitly bound in the issued contract and consumption
   receipt.

No architectural gap justified a new conversational adapter or dynamic provider
abstraction.
